<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css">
    <link rel="stylesheet" href="./css/archive.css">
    <title></title>
</head>
<body>
    <div class="tab-pane fade" id="pills-signup" role="tabpanel" aria-labelledby="pills-signup-tab">
        <div class="col-sm-12 border border-success shadow">

            <form id="singnupFrom" action="/signup_auth" method="POST">
                <div class="row">
                    <div class="form-group col-6">
                        <label class="font-weight-bold">First Name <span
                                class="text-danger">*</span></label>
                        <input type="text" name="signup_first_name" id="signup_first_name"
                            class="form-control" placeholder="First name..." autocomplete="off" quired>
                    </div>

                    <div class="form-group col-6">
                        <label class="font-weight-bold">Last Name <span class="text-danger">*</span></label>
                        <input type="text" name="signup_last_name" id="signup_last_name"
                            class="form-control" placeholder="Last name..." autocomplete="off" required>
                    </div>

                    <div class="form-group col-6">
                        <label class="font-weight-bold">Email<span class="text-danger">*</span></label>
                        <input type="email" name="signup_email" id="signup_email" class="form-control"
                            placeholder="Enter valid email" autocomplete="off" required>
                    </div>

                    <div class="form-group col-6">
                        <label class="font-weight-bold">Password <span class="text-danger">*</span></label>
                        <input type="password" name="signupcpassword" id="signupcpassword"
                            class="form-control" placeholder="******" autocomplete="off" pattern="^\S{6,}$"
                            onchange="this.setCustomValidity(this.validity.patternMismatch ? 'Must have at least 6 characters' : ''); if(this.checkValidity()) form.password_two.pattern = this.value;"
                            required>
                    </div>

                    <div class="form-group">
                        <button type="submit" name="signup_submit" value="Sign Up"
                            class="btn btn-block btn-success signup" id="createBtn">Create Account
                        </button>
                    </div>

                </div>
            </form>
        </div>
    </div>
</body>
</html>